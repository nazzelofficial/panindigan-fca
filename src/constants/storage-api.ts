/**
 * Remote Storage API configuration.
 *
 * These environment variables are optional. When omitted, the
 * client automatically uses the built-in Storage API endpoints,
 * default timeout, and retry settings.
 *
 * @internal
 */